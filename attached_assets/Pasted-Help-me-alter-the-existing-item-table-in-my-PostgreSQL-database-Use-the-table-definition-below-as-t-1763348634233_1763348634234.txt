Help me alter the existing item table in my PostgreSQL database.
Use the table definition below as the authoritative schema and generate the full SQL migration script required to update the current item table so that it matches this structure exactly.

Important:

Do not delete or drop the table.

Use ALTER TABLE, ADD COLUMN, ALTER COLUMN, DROP COLUMN, ADD CONSTRAINT, and CREATE INDEX commands as needed.

Ensure all constraints, data types, defaults, unique rules, check constraints, and foreign keys match what is shown.

Ensure all indexes (dk_item_1, dk_item_2, dk_item_3) are created if missing.

Ensure uppercase rules for item_code, item_name, and sku_code remain intact.

Ensure no data is lost.

Target schema (for alignment):

create table item (
  item_id integer not null generated by default as identity
      constraint pk_item primary key,

  item_code varchar(16) not null
      constraint c_item_1a check (item_code = upper(item_code))
      constraint uk_item_1 unique,

  item_name varchar(60) not null
      constraint c_item_1b check (item_name = upper(item_name))
      constraint uk_item_2 unique,

  sku_code varchar(30) not null
      constraint c_item_1c check (sku_code = upper(sku_code))
      constraint uk_item_3 unique,

  category_id integer not null
      constraint fk_item_itemcatg references itemcatg,

  item_desc citext not null,

  reorder_qty decimal(12,2) not null
      constraint c_item_1d check (reorder_qty > 0.00),

  default_uom_code varchar(25) not null
      constraint fk_item_unitofmeasure references unitofmeasure,

  units_size_vary_flag boolean not null default FALSE,
  usage_desc text,
  storage_desc text,
  is_batched_flag boolean not null default TRUE,
  can_expire_flag boolean not null default FALSE,

  issuance_order varchar(20) not null default 'FIFO',
  comments_text text,

  status_code char(1) not null
      constraint c_item_3 check (status_code in ('A','I')),

  create_by_id varchar(20) not null,
  create_dtime timestamp(0) without time zone not null,
  update_by_id varchar(20) not null,
  update_dtime timestamp(0) without time zone not null,
  version_nbr integer not null
);

create index dk_item_1 on item(item_desc);
create index dk_item_2 on item(category_id);
create index dk_item_3 on item(sku_code);


Deliverables:

A clean PostgreSQL migration script using ALTER TABLE statements to bring the current table in line with the target schema.

Validation steps to verify that all fields, constraints, and indexes exist after the migration.

Do not modify or remove any existing data.